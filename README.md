# 📱 Telegram Form Integration - Полное руководство

## 📋 Содержание

1. [Обзор](#обзор)
2. [Быстрый старт](#быстрый-старт)
3. [Файлы проекта](#файлы-проекта)
4. [Client-side решение](#client-side-решение)
5. [Server-side решение](#server-side-решение)
6. [Безопасность](#безопасность)
7. [Тестирование](#тестирование)
8. [FAQ](#faq)

---

## 🎯 Обзор

Проект реализует интеграцию контактных форм сайта с Telegram ботом для получения заявок.

### Реквизиты

- **Bot Token:** `8027049517:AAFF3XdvDk7epuTXXpgjYXWRKYV69xMIz4M`
- **Chat ID:** `771386337`

### Формат сообщения

```
🚀 Новая заявка с сайта

👤 Имя: [значение]
📞 Контакт: [значение]
📝 Текст: [значение]

⏰ Время: [дата и время]
```

---

## 🚀 Быстрый старт

### Вариант 1: Client-side (уже настроен)

1. **Откройте тестовую страницу:**

   ```
   test-telegram.html
   ```

2. **Или используйте основной сайт:**

   ```
   index.html
   ```

3. **Заполните форму и отправьте**

4. **Проверьте Telegram**

### Вариант 2: Server-side (для production)

1. **Установите зависимости:**

   ```bash
   npm install
   ```

2. **Настройте .env:**

   ```bash
   cp .env.example .env
   # Отредактируйте .env при необходимости
   ```

3. **Запустите сервер:**

   ```bash
   npm start
   ```

4. **Обновите client-side скрипт:**
   Измените URL в `telegram-integration.js`:

   ```javascript
   const API_URL = 'http://localhost:3000/api/contact';
   ```

---

## 📁 Файлы проекта

### Основные файлы

| Файл | Описание |
|------|----------|
| `index.html` | Основной сайт с интегрированной формой |
| `telegram-integration.js` | Client-side скрипт интеграции |
| `test-telegram.html` | Тестовая страница для проверки |

### Документация

| Файл | Описание |
|------|----------|
| `README.md` | Этот файл - полное руководство |
| `README-TELEGRAM.md` | Детальная документация интеграции |
| `QUICKSTART.md` | Краткое руководство по запуску |

### Server-side (опционально)

| Файл | Описание |
|------|----------|
| `server-example.js` | Пример серверной реализации |
| `package.json` | Зависимости для сервера |
| `.env.example` | Пример конфигурации |

---

## 💻 Client-side решение

### Преимущества

- ✅ Простота реализации
- ✅ Не требует сервера
- ✅ Работает сразу "из коробки"
- ✅ Подходит для статических сайтов

### Недостатки

- ⚠️ Bot Token виден в коде
- ⚠️ Возможность злоупотребления
- ⚠️ Ограниченная защита

### Функции безопасности

- ✅ Rate limiting (3 запроса в минуту)
- ✅ Валидация данных
- ✅ Экранирование HTML (защита от XSS)
- ✅ Проверка на подозрительный контент

### Как работает

```javascript
// 1. Обнаружение форм
MutationObserver → находит формы

// 2. Перехват отправки
form.addEventListener('submit', handler)

// 3. Сбор данных
const data = { name, contact, message }

// 4. Валидация
validateFormData(data)

// 5. Отправка в Telegram
fetch('https://api.telegram.org/bot.../sendMessage')

// 6. Уведомление пользователя
showNotification(result)
```

---

## 🔒 Server-side решение

### Преимущества

- ✅ Bot Token скрыт на сервере
- ✅ Полный контроль над запросами
- ✅ Расширенная валидация
- ✅ Логирование
- ✅ IP-адрес отправителя

### Архитектура

```
[Браузер] → [Ваш сервер] → [Telegram API]
   ↓              ↓
  AJAX      Валидация
           Rate Limiting
           Логирование
```

### Установка

1. **Клонируйте файлы:**

   ```bash
   # Файлы уже в папке проекта
   ```

2. **Установите Node.js:**
   - Скачайте с [nodejs.org](https://nodejs.org)
   - Версия: 14.0.0 или выше

3. **Установите зависимости:**

   ```bash
   npm install
   ```

4. **Настройте .env:**

   ```bash
   cp .env.example .env
   nano .env  # или любой редактор
   ```

5. **Запустите:**

   ```bash
   # Production
   npm start

   # Development (с auto-reload)
   npm run dev
   ```

### Обновление client-side

Измените `telegram-integration.js`:

```javascript
// Было:
const url = `https://api.telegram.org/bot${BOT_TOKEN}/sendMessage`;

// Стало:
const url = 'http://localhost:3000/api/contact';

// И измените метод отправки:
async function sendToTelegram(formData) {
    try {
        const response = await fetch(url, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify(formData)
        });
        
        const data = await response.json();
        return data;
    } catch (error) {
        console.error('Error:', error);
        return { success: false, message: error.message };
    }
}
```

---

## 🛡️ Безопасность

### Client-side

#### Реализовано

- ✅ Rate limiting (3 запроса/минуту)
- ✅ Валидация полей
- ✅ Экранирование HTML
- ✅ Проверка на XSS

#### Рекомендации

- ⚠️ Используйте только для тестирования
- ⚠️ Для production используйте server-side
- ⚠️ Регулярно меняйте Bot Token
- ⚠️ Ограничьте права бота

### Server-side

#### Реализовано

- ✅ Скрытие Bot Token
- ✅ Express Rate Limiting
- ✅ Валидация данных
- ✅ Логирование запросов
- ✅ IP-адрес отправителя

#### Дополнительно рекомендуется

- 🔧 HTTPS (SSL сертификат)
- 🔧 CAPTCHA (Google reCAPTCHA)
- 🔧 Honeypot поля
- 🔧 CORS настройки
- 🔧 Helmet.js для безопасности заголовков

---

## 🧪 Тестирование

### 1. Тестовая страница

```bash
# Откройте в браузере
test-telegram.html
```

**Что проверить:**

- [ ] Форма отображается
- [ ] Валидация работает
- [ ] Кнопка блокируется при отправке
- [ ] Уведомление появляется
- [ ] Сообщение приходит в Telegram
- [ ] Форма очищается после отправки

### 2. Основной сайт

```bash
# Откройте в браузере
index.html
```

**Что проверить:**

- [ ] Форма найдена автоматически
- [ ] Интеграция работает
- [ ] Консоль показывает логи

### 3. Консоль браузера (F12)

**Ожидаемые логи:**

```
🚀 Telegram Integration: Initializing...
✅ Telegram Integration: Ready
✅ Telegram Integration: Form integrated
Message sent successfully: [время]
```

### 4. Rate Limiting

**Тест:**

1. Отправьте 3 формы подряд
2. Попробуйте отправить 4-ю
3. Должно появиться: "Слишком много запросов"

### 5. Валидация

**Тест:**

- Пустое имя → ошибка
- Короткий контакт → ошибка
- XSS попытка → блокировка

---

## ❓ FAQ

### Сообщение не приходит в Telegram

**Решение:**

1. Проверьте Bot Token и Chat ID
2. Убедитесь, что вы написали боту хотя бы раз
3. Проверьте консоль на ошибки
4. Проверьте интернет-соединение

### "Слишком много запросов"

**Решение:**

- Подождите 1 минуту
- Или измените лимит в коде:

  ```javascript
  const RATE_LIMIT = {
      maxRequests: 5,  // увеличьте
      timeWindow: 60000
  };
  ```

### Форма не найдена

**Решение:**

1. Проверьте, что форма существует на странице
2. Проверьте консоль на логи
3. Увеличьте задержку:

   ```javascript
   setTimeout(() => {
       // ...
   }, 2000); // было 1000
   ```

### Как изменить формат сообщения?

**Решение:**
Отредактируйте функцию `formatMessage` в `telegram-integration.js`:

```javascript
function formatMessage(formData) {
    return `
🎯 Новая заявка

Имя: ${formData.name}
Email: ${formData.contact}
Сообщение: ${formData.message}
    `.trim();
}
```

### Как добавить больше полей?

**Решение:**

1. Добавьте поля в HTML форму
2. Обновите сбор данных:

   ```javascript
   const formData = {
       name: form.querySelector('[name="name"]')?.value,
       contact: form.querySelector('[name="contact"]')?.value,
       message: form.querySelector('[name="message"]')?.value,
       company: form.querySelector('[name="company"]')?.value, // новое
   };
   ```

3. Обновите `formatMessage`

### Как использовать с другим ботом?

**Решение:**

1. Создайте нового бота через @BotFather
2. Получите новый Bot Token
3. Получите новый Chat ID
4. Обновите `TELEGRAM_CONFIG` в коде

---

## 📞 Поддержка

### Текущая конфигурация

- **Bot Token:** `8027049517:AAFF3XdvDk7epuTXXpgjYXWRKYV69xMIz4M`
- **Chat ID:** `771386337`

### Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [BotFather](https://t.me/BotFather)
- [Express.js](https://expressjs.com/)

---

## 📝 Лицензия

Данный код предоставляется "как есть" для использования в проекте.

---

## ✅ Чек-лист готовности

- [x] Client-side скрипт создан
- [x] Скрипт подключен к index.html
- [x] Валидация добавлена
- [x] Rate limiting настроен
- [x] Уведомления работают
- [x] Тестовая страница создана
- [x] Server-side пример создан
- [x] Документация написана
- [x] Примеры конфигурации добавлены

## 🎉 Готово к использованию

Интеграция полностью настроена и готова к работе!
